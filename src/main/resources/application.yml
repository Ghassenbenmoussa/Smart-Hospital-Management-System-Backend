server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:postgresql://thomas.proxy.rlwy.net:55904/railway
    username: postgres
    password: XCwAKzpnpvRohlztulsNsGgUCsbYYrgt
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect